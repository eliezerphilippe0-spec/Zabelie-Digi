# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# MonCash (rail de paiement MVP — voir docs/03-PAIEMENTS.md)
# NE PAS committer de vraies clés. Dépendances NatCash/BRH différées (docs §14).
MONCASH_CLIENT_ID=
MONCASH_CLIENT_SECRET=
MONCASH_MODE=sandbox

# Rails diaspora USD (optionnels — non renseignés = rail masqué au checkout)
# Taux de conversion HTG → USD (ex. 132 = 132 HTG pour 1 USD). Requis pour
# activer Stripe ou Zelle : le montant USD est figé au checkout puis vérifié
# en base (payments.expected_usd_cents).
USD_HTG_RATE=

# Stripe (⚠️ nécessite une entité US — Haïti non supporté comme pays marchand).
# Webhook à créer côté Stripe : https://<domaine>/api/stripe/webhook,
# événement checkout.session.completed.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Zelle (flux semi-manuel : instructions + confirmation admin).
# ZELLE_RECIPIENT = e-mail/téléphone US enrôlé Zelle ; NAME = titulaire affiché.
ZELLE_RECIPIENT=
ZELLE_RECIPIENT_NAME=

# Recharge téléphonique (V-11 — revendeur Reloadly, docs/07-TOPUP.md)
# Non renseigné = service masqué (/rechaj affiche « à venir »).
RELOADLY_CLIENT_ID=
RELOADLY_CLIENT_SECRET=
RELOADLY_MODE=sandbox

# E-mails transactionnels (V-13 — livraison acheteur + notification vendeur).
# Non renseigné = aucun e-mail (le paiement fonctionne sans).
# Compte gratuit : https://resend.com (3 000 e-mails/mois).
#
# ⚠️ LES DEUX, PAS UNE. Sans EMAIL_FROM, l'expéditeur retombe sur
# `onboarding@resend.dev` — le BAC À SABLE de Resend, qui ne livre qu'à
# l'adresse du titulaire du compte. La clé peut être parfaitement valide et
# aucun acheteur ne reçoit rien. Verdict en un appel : /api/admin/email-verify.
#
# Le domaine d'EMAIL_FROM doit être ajouté chez Resend ET vérifié par ses
# enregistrements DNS, sinon chaque envoi est refusé.
RESEND_API_KEY=
EMAIL_FROM=Zabelie <notifications@votre-domaine.com>

# Réconciliateur (/api/reconcile)
# - RECONCILE_SECRET : appels manuels (POST, Bearer ou x-reconcile-secret).
# - CRON_SECRET       : injecté par Vercel Cron dans l'en-tête Authorization (GET).
RECONCILE_SECRET=
CRON_SECRET=

# App
# URL publique du site — sert à TOUT ce qui doit être absolu : renvoi après
# connexion, retour de paiement, og:image, canoniques.
# ⚠️ Non renseignée en déploiement, le code retombe sur VERCEL_URL (donc
#    *.vercel.app) et chaque renvoi part là-bas.
# ⚠️ NEXT_PUBLIC_* est figée au BUILD : la changer sans REDÉPLOYER ne change
#    rien. Et ne pas coller la valeur — la retaper (un \n invisible produit
#    une variable présente, invalide, et sans aucune trace : docs/38 §4).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Capteur de demande (lot S, migration 0047 — docs/18).
# Poivre serveur de l'empreinte de session. 16 caractères MINIMUM, propre à
# l'environnement. AUCUN repli n'est prévu : sans cette variable, aucune
# recherche sans résultat n'est journalisée — et ce silence est lui-même
# journalisé, pour qu'on ne confonde pas « personne n'a cherché » avec « la
# collecte est éteinte ». Ne JAMAIS la dériver de SUPABASE_SERVICE_ROLE_KEY :
# faire tourner la clé de service ré-anonymiserait tout l'historique.
SEARCH_FINGERPRINT_SALT=

# Vente de recharge EN PROPRE par Zabelie (V-11) — FERMÉE par décision du
# porteur (V-17, 2026-08-01) : Zabelie est un intermédiaire, pas un marchand.
# Défaut = fermé, y compris si la variable est absente. Seule la chaîne exacte
# `true` rouvre. Le code n'est pas supprimé — `0010`/`0029` sont appliquées et
# des commandes payées peuvent exister : leur suivi et leur remboursement
# restent ouverts quoi qu'il arrive. → docs/02-DECISIONS.md V-17.
ZABELIE_TOPUP_FIRSTPARTY_ENABLED=false

# ── Ajoutées le 2026-08-20 (audit docs/41 §7.5) ─────────────────────────────
# Onze variables étaient LUES par le code sans figurer ici : un nouvel
# environnement démarrait amputé sans que rien ne le dise. Le croisement est
# désormais tenu par tests/env-example-complet.test.ts, dans les deux sens.

# Aide à la description (V-12). Un seul fournisseur suffit ; sans aucun des
# deux, l'assistant ne répond plus — le reste du dépôt fonctionne.
OPENAI_API_KEY=
OPENAI_MODEL=
GEMINI_API_KEY=
GEMINI_MODEL=

# Canal de contact affiché dans l'interface. Non renseigné = le canal
# disparaît de l'écran, sans erreur.
NEXT_PUBLIC_WHATSAPP_NUMBER=
NEXT_PUBLIC_WHATSAPP_LINK=
NEXT_PUBLIC_CONTACT_EMAIL=

# Transformation d'images Supabase (redimensionnement à la volée).
# Non renseignée = les images sont servies à leur taille d'origine — plus
# lourdes, ce qui compte sur 3G.
NEXT_PUBLIC_SUPABASE_IMAGE_TRANSFORM=

# ⛔ JAMAIS EN PRODUCTION. Fait servir le catalogue de DÉMONSTRATION
# (lib/products.ts:82) : de faux produits devant de vrais acheteurs.
# Réservé au harnais e2e.
ZABELIE_DEMO_FIXTURES=
